import React from 'react'

import './index.css'

const MiniCarousel = ({ slider }) => {
    return (
        <section className="mini__carousel">
            {slider}
        </section>
    )
}

export default MiniCarousel
